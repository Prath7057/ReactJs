import React from 'react';

const FirstApp = ({ counter }) => {
  return (
    <div className="container mt-5">
      <h1 className="text-center">{counter}</h1>
    </div>
  );
};

export default FirstApp;
